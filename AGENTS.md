# ドキュメント運用方針

- `docs/` には、現在のプロジェクトで正式に採用・決定された情報のみを配置する。
  - `requirements/`: 確定した要件
  - `architecture/`: 現行のシステムアーキテクチャ
  - `design/`: 基本設計・詳細設計
  - `decisions/`: ADRなどの意思決定記録
  - `operations/`: リリース・運用に関する現行情報
- `research/` には、調査資料、検討途中の情報、採用しなかった案などを配置する。配下の情報は現行仕様・設計の根拠として扱わない。
- Codexが実装・設計を行う際は、`docs/` 配下を現在の正として扱う。
- `docs/` に必要な情報が存在しない場合は、`research/` の情報から推測して実装せず、ユーザーに確認する。
- `research/` を参照するのは、ユーザーから明示的に指示された場合のみとする。
